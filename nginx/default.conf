upstream django {
    server django_app:8000;
}

server {
    location / {
        proxy_pass http://django;
    }

    location /static/ {
        alias /app/setup/static/;
    }

    location /media/ {
        alias /app/setup/static/images;
    }
}